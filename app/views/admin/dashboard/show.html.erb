<div class="container text-center">
  <ul class="nav nav-tabs" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" data-toggle="tab" href="#users" role="tab">Users</a>
    </li>
    <li class="nav-item" id="companies-tab">
      <a class="nav-link" data-toggle="tab" href="#companies" role="tab">Companies</a>
    </li>
  </ul>

  <div class="tab-content">
    <div class="tab-pane active" id="users" role="tabpanel">
      <table class="table">
        <thead>
          <th>User</th>
          <th>Active?</th>
        </thead>

        <tbody>
          <% @users.each do |user| %>
          <tr>
            <th scope="row"><%= user.username unless user.admin? %></th>
              <td><%= button_to "Mark As Paid", order_path(id: order.id, new_status: "paid"), method: :patch, class: "btn btn-default" if user.active? %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="tab-pane" id="companies" role="tabpanel">
      <% @companies.each do |company| %>
      <% end %>
    </div>
  </div>
</div>
