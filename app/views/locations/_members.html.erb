<ul>
  <% location.users.each do |user| %>
    <li><strong><%= link_to user.username, user_path(user) %></strong></li>
    <li><strong><%= link_to user.username, user_path(user) %></strong></li>
  <% end %>
</ul>
